@using Sales_Inventory.Models.DTO;
@model PaginationDTO<ReceiptsDTO>;


<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>OR Type</th>
            <th>Series From</th>
            <th>Series To</th>
            <th>Date Added</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.item_list.Count() > 0)
        {
            foreach (var item in Model.item_list)
            {
                <tr>

                    <td>@item.type</td>
                    <td>@item.or_from</td>
                    <td>@item.or_to</td>
                    <td>@item.date_added</td>
                    <td style="width:10%">
                        <div class="row">
                            <div class="col-lg-12 pb-1">
                                <button type="button" class="btn btn-success btn-sm col-12" onclick="GetById(@item.receipt_id)">
                                    <i class="fa-solid fa-pen-to-square"></i>   Edit
                                </button>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center">No Data Available</td>
            </tr>
        }
    </tbody>
</table>
<nav>
    <ul class="pagination justify-content-end">
        <li class="page-item disabled">
            <a class="page-link" href="#" aria-label="Previous">
                <span aria-hidden="true">&laquo;</span>
            </a>
        </li>
        @for (int i = 1; i <= Model.page_count; i++)
        {
            <li class="page-item @(i == ViewBag.Page ? "active" :"")"><a class="page-link" onclick="PaginationClick(@i)">@i</a></li>
        }
        <li class="page-item disabled">
            <a class="page-link" href="#" aria-label="Next">
                <span aria-hidden="true">&raquo;</span>
            </a>
        </li>
    </ul>
</nav>

